tx_shibboleth {
	FE {
		IDMapping {
			shibID = TEXT
			shibID.field = REMOTE_USER
			typo3Field = username
		}
		userControls {
			allowUser = TEXT
			allowUser.value = 1
			createUserFieldsMapping {
				# initial user group; TYPO3 needs at least one for login
				usergroup = TEXT
				usergroup.value = 1

				email = TEXT
				email.field = REMOTE_USER
				first_name = TEXT
				first_name.value = Vorname
				middle_name = TEXT
				middle_name.value = M.
				last_name = TEXT
				last_name.value = Nachname
			}
			updateUserFieldsMapping {
				# Always update email and Name from IdP
				email = TEXT
				email.field = REMOTE_USER
				name = TEXT
				name.value = Testname
				first_name = TEXT
				first_name.value = Vorname
				middle_name = TEXT
				middle_name.value = M.
				last_name = TEXT
				last_name.value = Nachname
			}
		}
	}
	BE {
		IDMapping {
			shibID = TEXT
			shibID.field = REMOTE_USER
			typo3Field = username
		}
		userControls {
			allowUser = TEXT
			allowUser.value = 1
			createUserFieldsMapping {
				# Don't create an admin
				admin = TEXT
				admin.value = 0
				# Mount from groups: DB Mounts & File Mounts
				options = TEXT
				options.value = 3

				email = TEXT
				email.field = REMOTE_USER
				realName = COA
				realName.1 = TEXT
				realName.1.value = Nachname
				realName.3 = TEXT
				realName.3.value = Vorname
				realName.3.noTrimWrap  = |, ||
			}
			updateUserFieldsMapping {
				# Always update email and Name from IdP
				email = TEXT
				email.field = REMOTE_USER
				realName = COA
				realName.1 = TEXT
				realName.1.value = Nachname
				realName.3 = TEXT
				realName.3.value = Vorname
				realName.3.noTrimWrap  = |, ||
			}
		}
	}
}